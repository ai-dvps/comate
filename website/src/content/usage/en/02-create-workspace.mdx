---
title: Create a workspace
step: 2
---

Click **New Workspace** in the sidebar and choose a local project folder. Each workspace remembers its own settings, sessions, and configuration, so you can switch between projects easily.

After creating a workspace, open its settings to configure the model, API keys, skills, MCP servers, and WeCom bot options.
