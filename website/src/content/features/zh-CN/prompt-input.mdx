---
title: 提示输入与发现
description: 自动扩展的多行输入框、斜杠命令/Skill 发现、@ 文件自动补全、提示历史回呼和可配置发送快捷键，提升输入效率。
category: 效率工具
order: 5
icon: prompt-input
image: /comate/images/features/prompt-input.svg
imageAlt: 提示输入与发现截图
---

输入框随内容自动增高，支持 `/` 触发 Skill/命令选择器和 `@` 触发文件引用。可解析的 Skill 和工作区文件会显示为轻量 chip，避免被意外修改一部分；如需替换，先用 Backspace 或 Delete 整体删除，再重新输入 `/` 或 `@`。提示历史可模糊搜索并快速回填；可切换 Enter 发送或 Ctrl/Cmd+Enter 发送，并保护 IME 组合输入。
