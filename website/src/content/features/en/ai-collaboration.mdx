---
title: Choose Agent and model independently
description: Select the Agent backend that executes the task, then configure model access separately through a Provider that fits your personal or enterprise environment.
category: Control execution and evidence
order: 3
icon: interactive-surfaces
image: /comate/images/features/ai-collaboration.svg
imageAlt: AI collaboration screenshot
---

An **Agent backend** is the task execution engine. A **Provider** is the model-service configuration and credentials it can use. Comate includes no free inference: a Workspace and draft Session can exist first, but Agent execution cannot complete until a Provider is configured.
