---
title: Files & Context
description: Browse the workspace tree, search files with ripgrep, preview files in a resizable panel, pin files for context, and reference them with @ mentions or clickable relative paths.
category: Core
order: 4
icon: file-explorer
image: /comate/images/features/files.svg
imageAlt: Files and context screenshot
---

The sidebar file explorer supports directory navigation; file search uses on-demand ripgrep indexing. A resizable file panel supports multiple tabs, syntax highlighting, and Markdown preview. Pin files to the side panel as chat context or reference them quickly with `@` in the prompt input.
