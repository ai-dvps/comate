# Quota lookup fixture

Use Comate's `authenticatedRequest` MCP tool with the sanitized quota recipe supplied by the current task. Keep `authBinding` opaque. Never print, export, or persist authentication material. If the binding is stale, ask the user to reopen, sign in, capture the quota action again, and explicitly remember the site only if they want persistence.
