/**
 * Shared focus-visible ring for the browser pane/popout interactive elements.
 * One definition so the four browser components cannot drift apart.
 */
export const FOCUS_CLASSES =
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/60'
